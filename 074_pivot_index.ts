function pivotIndex(arr: number[]): number {
    let minSoFar = Infinity;
    let maxOfRest = -Infinity;

    let minArrs

    for (let i = 0; i < arr.length; i++) {
        minSoFar = Math.min(minSoFar, arr[i]);
        maxSoFar = Math.max(maxSoFar, arr[i]);
        if ( minSoFar)
    }
}
